# Check if an argument is url string

Check if an argument is url string

## Usage

``` r
is_url(x)
```

## Arguments

- x:

  (`any`)  
  Object to check.

## Value

`TRUE` if `x` is a valid URL string, `FALSE` otherwise.
