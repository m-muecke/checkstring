# Check if an argument is a CUID2 string

Check if an argument is a CUID2 string

## Usage

``` r
is_cuid2(x)
```

## Arguments

- x:

  (`any`)  
  Object to check.

## Value

`TRUE` if `x` is a valid CUID2 string, `FALSE` otherwise.
