# Check if an argument is base64url string

Check if an argument is base64url string

## Usage

``` r
is_base64url(x)
```

## Arguments

- x:

  (`any`)  
  Object to check.

## Value

`TRUE` if `x` is a valid base64url string, `FALSE` otherwise.
