# Check if an argument is an UUID string

Check if an argument is an UUID string

## Usage

``` r
is_uuid(x)
```

## Arguments

- x:

  (`any`)  
  Object to check.

## Value

`TRUE` if `x` is a valid UUID string, `FALSE` otherwise.
